Привет, Егор! (Ещё раз)

Заменил предусмотренный тестом _fill_initial_data на собственный метод generate_data_for_test_func() - мой метод создаёт файлы по тем же данным, которые написаны в тесте, при этом пересоздавая их каждый раз для того, чтобы методы работали с "чистым" списком, с его изначальным видом. С методом _fill_initial_data приходилось перезаливать файлы .txt в нужную папку после каждого теста.

+Закомментировал некоторые исключения из своего кода для корректности теста (Мне показалось, что было бы интересно добавить исключение ValueError с текстом типа "Проверьте значение VIN", если переданный VIN не был найден в индексе - одна тесту же нужно, чтобы в таком случае возвращался None. 

ВАЖНО: Прошу выполнять код исключительно с тем окружением, которое лежит в папке "For tests" - или создать там новое, чтобы методам передавался путь этой же самой директории. 

Про генерацию данных:
Изначально создаются только файла для авто и моделей + пустые файлы продаж - в процессе тестов они успешно заполняются